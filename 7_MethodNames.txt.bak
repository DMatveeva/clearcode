1) updateLots -> createLots and updateLots // Сейчас метод выполняет 2 фунции, создание и изменение лотов. Лучше разделить их.
2) conductTest -> runTest and writeTestToFile // Сейчас метод выполняет 2 функции: прогоняет тест и пишет результаты в файл. 
3) createPerson -> addNewPerson // Метод в классе Environment, который выполняет 3 функции: создает участника и добавляет его в группу и стратегию. Лучше переименовать метод, а внутри вызывать 3 отдельных метода: createPerson для создания участника с заданными параметрами и addPersonTOGroup, addPersonToStrategy
4) createGroup -> addNewGroup // Метод в классе Environment, который создает группу и добавляет ее в список групп для данной среды. В новом названии понятно что добавляется именно новая группа, а не просто создается.
5) doStep -> holdVotingRound // Шаг в среде - это раунд голосования, поэтому так и лучше переименовывать, вместо абстрактного шага.
6) countIncrement -> calculateIncrement // Count звучит так, как будто считаем количество приращений, а на самом деле вычисляем приращение.
7) Table.delete() -> Table.deleteLastLine()  // Более конкретно, удаляется именно последняя строка из таблицы.
8) convertWithOverall -> convert // Единственный метод в классе ParticipantToResultConverter. Поэтому непонятно, зачем постфикс withOverall. 
9) isParticipant -> isGroupMember // Метод определяет, принадлежит ли участник к указанной группе. Везде в проекте ипользуется слово Group, поэтому логично его использовать в названии метода.  
10) makeTest -> conductVotingExperiment // Метод в классе ExperimentEnvironment, который проводит эксперимент с голосованием множества участников. Новое название отражает суть.
11) TableComparator.compareTwoFiles -> readTableFromFile and compareTables // Сейчас метод читает таблицы из двух разных файлов и сравнивает строки. Тут 2 функции у одного метода. И к тому же название метода не соответствует названию класса. Лучше разделить на два метода с сответствующими функциями и названиями.
12) parseFile -> loadParamsFromFile // Метод, который считывает параметры эксперимента из файла. 
